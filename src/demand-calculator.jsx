import React from 'react';
import { css } from 'glamor';

export default function DemandCalculator() {

  let grid = style({
    display: 'grid',
    gridTemplateColumns: 'auto',
  });

  buildTable() {
    return (
      <table>

      </table>
    )
  }

  return (<div>
    This is demand
  </div>)

}